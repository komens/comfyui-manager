参考 提交到Comfyui作图的逻辑，帮我封装一个可视化的comfyui作图提交与结果查看应用（go + web + docker化，用于nas部署）。
项目根目录为：comfyui-server（前后端都在里面）
提交作图有三种方式：
一：使用json数据作图
    1. json数据来源：a.可以直接上传json文件 b.可以直接在应用中输入json数据和接送文件名(服务端生成对应的json文件)，文件名非必填。
    2. 在已上传的json列表中选择，将选中的json数据提交给comfyui。（需要标注出哪个json做过图，哪个json没做过图，同时支持选模工作流，和作图模式——全部重新生成还是仅制作未生成过的）
二：直接输入正向、负向提示词作图
    1. 直接在应用中输入正向、负向提示词，选择工作流，提交给comfyui

上面两种方案的具体参数需要根据实际情况进行调整。

其他功能：
1. 需要实时监控作图进度，已经生成的图片需要下载到本地，需要在应用中展示出来。
2. 每张图片需要按照提示词进行匹配，用户可以查看对应图片使用哪个提示词生成的。
3. 支持工作流参数配置（应用有多个工作流），用户可以在应用中配置工作流的参数，如采样步骤、 guidance scale 等。

后端使用 go，数据库使用 sqlite, 并将项目容器化，支持配置挂载路径，数据库和下载资源和json数据都需要挂载到外部磁盘，以避免数据丢失。
前端使用 Vue 进行编码，注意适配移动端和PC端。
