# ComfyUI 可视化作图应用程序开发细节

## 1. 文档定位

本文档是本项目的程序开发说明，明确模块边界、数据结构、接口、任务执行流程和页面实现方式，供后续编码直接使用。

本项目是个人使用的 NAS 应用，不设计多用户、权限系统、复杂工作流编排或云端部署能力。实现重点是：稳定提交 ComfyUI、保存结果、查看进度、断点恢复，以及尽量少的维护成本。

## 2. 最终功能范围

### 2.1 作图方式

支持以下入口：

1. 上传 JSON 文件后选择条目作图。
2. 在线粘贴或编辑 JSON 后保存，再选择条目作图。
3. 从已保存的 JSON 文件中选择全部、部分或未完成条目作图。
4. 直接输入正向提示词和负向提示词作图。

JSON 条目兼容现有脚本格式：

```json
{
  "negative": "global negative prompt",
  "entries": [
    {
      "id": "01-cafe-window",
      "title": "咖啡馆窗边",
      "desc": "午后咖啡馆窗边坐着喝咖啡",
      "positive": "positive prompt",
      "status": "done",
      "completed_at": "2026-09-10T12:00:00"
    }
  ]
}
```

同时兼容顶层直接为数组的旧格式。

### 2.2 任务功能

- 单条直接提示词任务。
- JSON 条目批量任务。
- 全部重新生成。
- 只生成未完成条目。
- 生成中查看进度。
- 任务失败后重试单条或全部失败条目。
- 生成完成后自动下载图片。
- 任务中断后恢复未完成项目。

### 2.3 图片功能

- 图片保存到 NAS 挂载目录。
- 网格查看图片。
- 查看原图和下载原图。
- 查看图片对应的正向提示词、负向提示词、工作流和参数。

### 2.4 明确不做

- 不做多用户、注册、登录和角色权限。
- 不做多个 ComfyUI 节点的负载均衡。
- 不做工作流可视化编辑器。
- 不做定时任务。
- 不做 Redis 等外部消息队列。
- 不单独安排测试阶段，只做开发过程中的必要手工验证。

### 2.5 ComfyUI 地址配置

ComfyUI 部署在内网，服务地址可能因为 DHCP 或网络环境变化而改变，不能写死在代码、Dockerfile 或工作流配置中。

需要支持以下配置方式：

- 首次启动时通过环境变量提供默认地址。
- 在应用设置页面中修改 ComfyUI 地址。
- 修改后保存到 SQLite，后续任务直接使用新地址。
- 提供“测试连接”按钮，显示 ComfyUI 是否可访问。
- 首页显示当前地址和连接状态。
- 地址变化只影响新提交和后续轮询中的请求，不修改历史任务记录。

地址格式示例：

```text
http://192.168.10.49:8188
http://192.168.10.87:8188
```

保存时需要自动处理：

- 去除末尾 `/`。
- 要求使用 `http://` 或 `https://`。
- 校验主机和端口格式。
- 禁止将路径拼接到 API 地址中。

运行中的任务需要保存提交时使用的地址快照，避免任务执行过程中修改地址后无法追踪任务来源；如果任务尚未提交，则使用当前最新地址。

## 3. 参数配置方案

### 3.1 结论

参数分为两类：

1. **工作流维护参数**：维护工作流时配置一次。
2. **本次生成参数**：每次提交任务时显示并确认，可以覆盖工作流默认值。

工作流维护页面保存节点映射、参数类型和默认值；提交页面读取这些配置生成表单，用户可以直接使用默认值，也可以临时修改本次任务的参数。临时修改不会改变工作流默认配置。

### 3.2 工作流维护时配置

每个工作流保存：

- workflow JSON 模板。
- 正向提示词节点和字段。
- 负向提示词节点和字段。
- seed 节点和字段。
- 输出文件名前缀节点和字段。
- steps、CFG、宽度、高度等参数节点和字段。
- 参数名称、类型、默认值、最小值、最大值。
- 参数是否在提交页面展示。

节点映射示例：

```json
{
  "positive_prompt": {"node_id": "4", "field": "inputs.text"},
  "negative_prompt": {"node_id": "5", "field": "inputs.text"},
  "seed": {"node_id": "2", "field": "inputs.seed"},
  "output_prefix": {"node_id": "8", "field": "inputs.filename_prefix"},
  "parameters": {
    "steps": {
      "node_id": "2",
      "field": "inputs.steps",
      "type": "integer",
      "default": 8,
      "min": 1,
      "max": 100
    },
    "cfg": {
      "node_id": "2",
      "field": "inputs.cfg",
      "type": "number",
      "default": 1.0,
      "min": 0,
      "max": 30
    }
  }
}
```

### 3.3 每次提交时配置

提交页面读取工作流默认参数，自动生成参数表单：

- `integer`：整数输入框。
- `number`：数字输入框。
- `boolean`：开关。
- `select`：下拉选择。
- `string`：文本输入框。
- `seed`：数字输入框和“随机”选项。

批量任务使用同一组本次参数。任务创建时保存最终参数快照，保证以后修改工作流默认值不会影响历史任务。

参数优先级：

```text
本次提交参数 > 工作流默认参数 > workflow JSON 原始值
```

## 4. 技术架构

```text
Vue 3 前端
    |
    | HTTP REST API + SSE
    v
Go 服务
    |
    +-- SQLite：工作流、JSON 文件、条目、任务、图片
    +-- 文件系统：JSON、workflow、图片、临时文件
    +-- 后台任务执行器
    +-- ComfyUI HTTP Client
    v
ComfyUI
```

### 4.1 后端目录

```text
backend/
├── cmd/server/main.go
├── internal/
│   ├── config/          # 环境变量和默认配置
│   ├── model/           # 数据库模型和状态常量
│   ├── repository/      # SQLite CRUD
│   ├── service/         # 文件、工作流、任务、图片业务
│   ├── comfyui/         # ComfyUI API 客户端
│   ├── scheduler/       # 后台任务执行和恢复
│   ├── storage/         # 文件保存、读取、下载
│   └── api/             # 路由、参数校验、响应
├── migrations/
├── Dockerfile
└── docker-compose.yml
```

### 4.2 前端目录

```text
frontend/
├── src/
│   ├── views/           # Dashboard、JSON、提交、任务、图片、工作流页面
│   ├── components/      # 参数表单、条目列表、进度条、图片卡片
│   ├── api/             # API 请求封装
│   ├── stores/          # 任务和工作流状态
│   ├── router/
│   └── types/
├── Dockerfile
└── nginx.conf
```

## 5. 数据库设计

使用 SQLite。数据库保存索引、状态和任务信息，图片和 JSON 原文件保存到外部目录。

### 5.1 `settings`

保存个人应用的运行配置。

```sql
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at DATETIME NOT NULL
);
```

至少保存：

```text
comfyui_url
max_concurrency
poll_interval
```

配置读取优先级：

```text
SQLite 运行时配置 > 环境变量初始值 > 程序默认值
```

首次启动时，如果 SQLite 中不存在 `comfyui_url`，就使用 `COMFYUI_URL` 写入 `settings`；之后用户在页面修改地址，不需要修改容器环境变量或重启容器。

### 5.2 `workflows`

```sql
CREATE TABLE workflows (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  workflow_path TEXT NOT NULL,
  mapping_json TEXT NOT NULL,
  enabled INTEGER NOT NULL DEFAULT 1,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

`mapping_json` 保存节点映射、参数定义和默认值。

### 5.3 `json_files`

```sql
CREATE TABLE json_files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  filename TEXT NOT NULL UNIQUE,
  storage_path TEXT NOT NULL,
  total_count INTEGER NOT NULL DEFAULT 0,
  completed_count INTEGER NOT NULL DEFAULT 0,
  failed_count INTEGER NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL
);
```

### 5.4 `prompt_entries`

```sql
CREATE TABLE prompt_entries (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  json_file_id INTEGER NOT NULL,
  entry_key TEXT NOT NULL,
  title TEXT NOT NULL DEFAULT '',
  description TEXT NOT NULL DEFAULT '',
  positive_prompt TEXT NOT NULL,
  negative_prompt TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'pending',
  completed_at DATETIME,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE(json_file_id, entry_key),
  FOREIGN KEY(json_file_id) REFERENCES json_files(id)
);
```

状态值：`pending`、`queued`、`running`、`success`、`failed`。

### 5.5 `generation_tasks`

```sql
CREATE TABLE generation_tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source_type TEXT NOT NULL,
  json_file_id INTEGER,
  workflow_id INTEGER NOT NULL,
  comfyui_url TEXT NOT NULL,
  mode TEXT NOT NULL DEFAULT 'pending',
  parameters_json TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending',
  total_count INTEGER NOT NULL DEFAULT 0,
  success_count INTEGER NOT NULL DEFAULT 0,
  failed_count INTEGER NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  started_at DATETIME,
  completed_at DATETIME,
  FOREIGN KEY(json_file_id) REFERENCES json_files(id),
  FOREIGN KEY(workflow_id) REFERENCES workflows(id)
);
```

`parameters_json` 保存本次任务的最终参数快照。直接提示词任务的正负提示词也保存在该 JSON 中。

### 5.6 `generation_items`

```sql
CREATE TABLE generation_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER NOT NULL,
  entry_id INTEGER,
  comfy_prompt_id TEXT,
  status TEXT NOT NULL DEFAULT 'queued',
  progress INTEGER NOT NULL DEFAULT 0,
  error_message TEXT NOT NULL DEFAULT '',
  started_at DATETIME,
  completed_at DATETIME,
  FOREIGN KEY(task_id) REFERENCES generation_tasks(id),
  FOREIGN KEY(entry_id) REFERENCES prompt_entries(id)
);
```

直接提示词任务没有 `entry_id`，但其提示词保存在所属任务的 `parameters_json`。

### 5.7 `images`

```sql
CREATE TABLE images (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  generation_item_id INTEGER NOT NULL,
  entry_id INTEGER,
  filename TEXT NOT NULL,
  storage_path TEXT NOT NULL,
  width INTEGER NOT NULL DEFAULT 0,
  height INTEGER NOT NULL DEFAULT 0,
  created_at DATETIME NOT NULL,
  FOREIGN KEY(generation_item_id) REFERENCES generation_items(id),
  FOREIGN KEY(entry_id) REFERENCES prompt_entries(id)
);
```

## 6. 文件目录设计

所有重要数据必须位于可挂载目录：

```text
/data/
├── db/comfyui.db
├── json/
│   ├── original/
│   └── backup/
├── workflows/
├── images/{task_id}/{entry_id}/
├── temp/
└── logs/
```

图片使用任务 ID 和条目 ID 分目录，避免同名文件覆盖。用户提交的文件名必须清理，禁止包含路径分隔符和 `..`。

## 7. ComfyUI 客户端实现

### 7.1 客户端接口

```go
type Client interface {
    Submit(ctx context.Context, prompt map[string]any, clientID string) (SubmitResult, error)
    History(ctx context.Context, promptID string) (HistoryResult, error)
    Download(ctx context.Context, image OutputImage, target string) error
    Queue(ctx context.Context) (QueueResult, error)
}
```

### 7.2 提交流程

1. 读取 workflow 文件并解析为 `map[string]any`。
2. 根据 `mapping_json` 找到节点和字段。
3. 写入正向提示词、负向提示词、seed 和参数。
4. 写入唯一的输出文件名前缀。
5. 组装请求：

```json
{
  "prompt": {},
  "client_id": "comfyui-server-task-12"
}
```

6. 使用任务创建时保存的 `comfyui_url`，POST 到 `${comfyui_url}/prompt`。
7. 保存响应中的 `prompt_id`。

### 7.3 状态查询

第一版使用轮询，避免引入额外复杂度：

- 每个项目每 5 秒查询一次 `/history/{prompt_id}`。
- 没有 history 记录时视为排队中。
- `status_str=success` 时进入下载。
- `status_str=error` 时记录错误并标记失败。
- 网络错误进行重试，连续失败后保留错误信息。

### 7.4 下载结果

从 history 的 `outputs` 中提取 `images`，通过以下接口下载：

```text
GET /view?filename=...&subfolder=...&type=...
```

下载成功后创建图片记录、更新项目状态、更新条目状态，并通过 SSE 推送事件。

## 8. 后台任务执行器

不使用 Redis，使用 Go channel 加 SQLite 持久化实现。

### 8.1 状态流转

```text
任务：pending -> running -> completed / failed / cancelled
项目：queued -> running -> success / failed
```

### 8.2 执行流程

1. API 创建任务记录。
2. 根据选择的条目创建 `generation_items`。
3. 将任务 ID 写入后台 channel。
4. worker 读取任务。
5. 逐条构造 workflow 并提交 ComfyUI。
6. 保存 `prompt_id` 并轮询状态。
7. 下载图片并保存记录。
8. 更新任务统计并推送进度。
9. 所有项目结束后更新任务状态。

第一版默认使用 1 个 worker。通过 `MAX_CONCURRENCY` 调整并发数，但个人 NAS 建议保持为 1。

### 8.3 服务重启恢复

启动时查询 `pending` 或 `running` 任务：

- `pending` 任务重新进入队列。
- 有 `comfy_prompt_id` 的项目先查询 history。
- 没有 prompt ID 且未成功的项目重新提交。
- 已经成功且有图片的项目不重复提交。

## 9. 后端 API

### 9.1 系统状态

```text
GET /api/health
GET /api/comfyui/status
GET /api/settings
PUT /api/settings/comfyui
POST /api/settings/comfyui/test
```

`PUT /api/settings/comfyui` 请求示例：

```json
{
  "url": "http://192.168.10.87:8188"
}
```

保存接口完成 URL 规范化和格式校验；测试接口向新的地址请求 ComfyUI 状态或队列接口，返回连接耗时和错误信息。任务创建时读取当前 URL 并写入 `generation_tasks.comfyui_url`，后台执行该任务时使用这个快照。

### 9.2 JSON 文件

```text
GET    /api/json-files
POST   /api/json-files/upload
POST   /api/json-files
GET    /api/json-files/:id
PUT    /api/json-files/:id
DELETE /api/json-files/:id
GET    /api/json-files/:id/entries
```

### 9.3 工作流

```text
GET    /api/workflows
POST   /api/workflows
GET    /api/workflows/:id
PUT    /api/workflows/:id
DELETE /api/workflows/:id
POST   /api/workflows/:id/validate
```

### 9.4 任务

```text
POST /api/tasks/json
POST /api/tasks/direct
GET  /api/tasks
GET  /api/tasks/:id
POST /api/tasks/:id/cancel
POST /api/tasks/:id/retry
GET  /api/tasks/:id/events
```

### 9.5 图片

```text
GET    /api/images
GET    /api/images/:id
GET    /api/images/:id/file
GET    /api/images/:id/download
DELETE /api/images/:id
```

### 9.6 SSE 进度事件

```text
GET /api/tasks/:id/events
```

事件示例：

```json
{
  "task_id": 12,
  "item_id": 35,
  "status": "success",
  "progress": 100,
  "success_count": 4,
  "failed_count": 0,
  "total_count": 10,
  "image_id": 88
}
```

## 10. 前端页面实现

### 10.1 路由

```text
/                       首页和任务概览
/json-files             JSON 文件列表
/json-files/new         新建 JSON
/json-files/:id         JSON 条目详情
/submit                 直接提示词提交
/submit/json/:id        JSON 批量提交
/tasks/:id              任务详情
/gallery                图片库
/workflows              工作流维护
/settings               应用设置
```

### 10.2 首页

显示当前 ComfyUI 地址、连接状态、当前运行任务、最近任务、待生成条目数、图片总数和最近图片。

### 10.3 应用设置页

提供：

- ComfyUI 地址输入框。
- 保存地址按钮。
- 测试连接按钮。
- 最近一次测试时间。
- 最近一次错误信息。
- 当前连接状态。

保存新地址后，前端刷新连接状态；后端后续创建的新任务使用新地址。已提交的任务继续使用任务记录中的地址快照。

### 10.4 JSON 文件页

提供上传、在线编辑、格式校验、条目列表、状态筛选、查看关联图片和删除功能。

条目行显示 ID、标题、描述、状态、图片数量和选择框，并提供“全部未完成”“仅失败”“仅已完成”等筛选。

### 10.5 提交页

工作流选择后读取参数 schema，动态生成参数表单。批量提交页额外显示条目选择和“全部重新生成/仅未完成”选项。

### 10.6 任务详情页

首次加载任务详情，随后建立 SSE 连接。收到项目完成事件后只刷新对应项目和图片；SSE 断开时保留手动刷新功能。

### 10.7 图片库

采用响应式网格和缩略图懒加载。图片详情显示关联的正向提示词、负向提示词、工作流、参数和任务时间。

### 10.8 移动端

- 条目表格在小屏幕改为卡片。
- 参数表单使用单列布局。
- 图片网格自适应列数。
- 提示词详情折叠显示。
- 375px 宽度下不出现横向滚动。

## 11. JSON 文件处理

### 11.1 上传和解析

1. 检查扩展名和文件大小。
2. 解析 JSON。
3. 判断顶层是对象还是数组。
4. 提取 `entries` 或顶层数组。
5. 校验每条记录的 `id`、`positive` 等字段。
6. 检查文件内 ID 是否重复。
7. 保存原始文件。
8. 写入 `json_files` 和 `prompt_entries`。

### 11.2 回写状态

生成成功后：

1. 先复制原文件到 `json/backup/`。
2. 根据 entry ID 更新 `status` 和 `completed_at`。
3. 写入临时文件。
4. 使用原子替换更新原文件。

## 12. Docker 和 NAS 实现

### 12.1 环境变量

```text
COMFYUI_URL=http://comfyui:8188
SERVER_PORT=8080
DATA_DIR=/data
DB_PATH=/data/db/comfyui.db
MAX_CONCURRENCY=1
POLL_INTERVAL=5s
MAX_UPLOAD_SIZE=50MB
```

`COMFYUI_URL` 只作为首次初始化或数据库配置丢失时的默认值。由于内网 IP 可能变化，日常修改应通过应用设置页完成，不要求重新构建镜像或修改代码。

### 12.2 Compose 要求

```yaml
services:
  comfyui-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      COMFYUI_URL: http://comfyui:8188
      DATA_DIR: /data
      DB_PATH: /data/db/comfyui.db
      MAX_CONCURRENCY: 1
      POLL_INTERVAL: 5s
    volumes:
      - ./data:/data
    restart: unless-stopped
```

实际 NAS 环境中将 `COMFYUI_URL` 替换为 ComfyUI 容器名、NAS 局域网 IP 或实际地址。

必须挂载：

- `/data/db`：SQLite 数据库。
- `/data/json`：JSON 文件。
- `/data/images`：生成图片。
- `/data/workflows`：工作流文件。
- `/data/logs`：日志文件。

## 13. 实际编码顺序

### 第一步：项目骨架

- 创建 Go 服务和 Vue 项目。
- 添加 SQLite 初始化和迁移。
- 添加环境变量配置。
- 添加 `settings` 表和 ComfyUI 地址初始化。
- 添加 Dockerfile、Compose 和 `/api/health`。

### 第二步：单张作图闭环

- 实现 ComfyUI 客户端。
- 客户端每次请求从任务快照读取 ComfyUI 地址。
- 读取 workflow JSON。
- 根据节点映射写入提示词和参数。
- 调用 `/prompt`。
- 轮询 `/history`。
- 调用 `/view` 下载图片。
- 保存任务和图片记录。
- 先用直接提示词页面验证闭环。

### 第三步：工作流维护

- 工作流 JSON 上传。
- 节点映射编辑。
- 默认参数编辑。
- 参数类型和范围保存。
- 工作流验证接口。
- 提交页面动态读取参数。

### 第四步：JSON 文件管理

- JSON 上传和在线编辑。
- JSON 解析和条目入库。
- 条目状态展示。
- JSON 文件备份和回写。

### 第五步：批量任务和恢复

- JSON 条目选择。
- 全部生成和未完成生成。
- 后台 worker。
- 任务和项目状态。
- 失败重试。
- 服务启动恢复未完成任务。

### 第六步：进度和图片库

- 任务详情页。
- SSE 进度推送。
- 图片缩略图和原图查看。
- 图片详情和提示词查看。
- 图片下载。

### 第七步：移动端和 NAS 部署

- 完成 PC 和移动端布局。
- 完善图片懒加载。
- 完善 Docker 构建。
- 验证目录挂载。
- 验证容器重启后数据和任务恢复。
- 验证修改随机内网 IP 后，新任务可以连接新的 ComfyUI 地址。
- 编写部署说明。

## 14. 第一版完成标准

1. 可以维护至少一个 ComfyUI workflow。
2. 可以配置 workflow 节点映射和默认参数。
3. 每次提交时可以查看并覆盖本次参数。
4. 可以直接输入正负提示词生成图片。
5. 可以上传 JSON 并选择条目批量生成。
6. 可以只生成未完成条目。
7. 生成图片会保存到外部挂载目录。
8. 页面可以显示任务进度和失败原因。
9. 图片可以查看对应提示词和生成参数。
10. 服务重启后数据库、JSON、图片和未完成任务不会丢失。
11. 页面可以在 PC 和手机浏览器使用。
12. Docker Compose 可以在 NAS 上启动应用。
13. ComfyUI 内网 IP 变化后，可以通过设置页面修改地址并成功提交新任务。
